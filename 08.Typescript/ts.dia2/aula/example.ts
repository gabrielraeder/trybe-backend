type bird = string;

type Sum = (x: number, y: number) => number;

type address = {
  street: string,
  number: number,
  city: string,
  state: string
}

type StatesOfMatter = "liquid" | "solid" | "gaseous";

type cpf = string | number;

type OStype = 'linux' | 'windows' | 'mac';

type vowels = 'a' | 'e' | 'i' | 'o' | 'u';